Bone Scan
LLM 통해서 핫스팟의 해부학적 위치로 표현해주는

accelerate launch 250504_ViT.py

허깅페이스 토큰은 dropbox BoneScan에 있음

ViT가 32float로 학습되어있음..

Lt. Rt.을 다 left, right으로 변경

단순 이름, method, Tc-99m 다 삭제

일단 너무 우유부단한 진단이 많음
암 환자인데 PET에서 나타나던 암전이 모습이 잘 안보인다 어찌고 저찌고..흠...
암이 있긴 한데 전에 비해서는 줄었다..

항상 소견은 suggestive of bone metastasis
suggestive of arthrisis
이렇게...


revised, modified 표현(수정된) 다 찾기

transUNet에 class imbalance weight넣는것도 해야됨 어찌되었든 배경이 너무 많기 때문에

84869#연구실

템플릿 registration??


python3 -m venv nhenv

normal, xnor 뭉뜽그려서 2class로 생각


20, 20

5, 5

tanh 통과, crop까지 완료하고 난 결과를 jpg로

MAS Segmentation

scp -r nhcho@103.22.220.93:/projects3/pi/nhcho/Sev_WBBS/skhyun/segmentation/Manual_Label/pre_jpg_folder ./

scp -P 3086 -r projects3/pi/nhcho/BS_80K_test/ nhcho@103.22.220.86:/projects1/pi/nhcho

ssh -L 5903:localhost:5903 nhcho@103.22.220.93 -p 3093

ssh nhcho@103.22.220.86 -p 3086

python ./labelme_rev.py ./pre_jpg_folder_orig/ ./250513_0405 --labels labels_rev.txt --noviz

normal : 17260
cancer : 9952
inf : 1823
어쨌든 엑셀파일의 label로 따라갈 것!!!!

768 512
384 256

nvidia-smi --query-compute-apps=pid --format=csv,noheader,nounits | while read pid; do
    if [ -d "/proc/$pid" ]; then
        uid=$(stat -c %u /proc/$pid)
        user=$(getent passwd "$uid" | cut -d: -f1)
        echo "USER: $user    PID: $pid"
    fi
done
이걸로 GPU 사용중인 user와 PID 추적 가능

awkward : 640

포트별 비번
93 PW : !dihv1902 (250502에 변경함)
86 PW : NhCho84869!